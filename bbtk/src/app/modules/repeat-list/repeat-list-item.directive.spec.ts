import { RepeatListItemDirective } from './repeat-list-item.directive';

describe('RepeatListItemDirective', () => {
  it('should create an instance', () => {
    const directive = new RepeatListItemDirective();
    expect(directive).toBeTruthy();
  });
});
